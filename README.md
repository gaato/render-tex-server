# render-tex-server

## Requirements
- Linux (or macOS)
- Python3
- TeXLive

## for Linux
```
$ pip -r requirements.txt
$ sudo adduser tex
$ cp tex /home/tex/tex
```
(Install Python3 as `tex`)
```
$ python main.py
```
